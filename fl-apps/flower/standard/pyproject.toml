# Copyright (c) 2026 Flower Labs GmbH
# Copyright (c) 2026 Guy's and St Thomas' NHS Foundation Trust & King's College London
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#     http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

# =====================================================================
# For a full TOML configuration guide, check the Flower docs:
# https://flower.ai/docs/framework/how-to-configure-pyproject-toml.html
# =====================================================================

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "standard-app"
version = "1.0.0"
description = "MONAI and Flower example based on spleen segmentation"
license = "Apache-2.0"
# Dependencies for your Flower App
dependencies = [
    "flip-utils>=0.1.8",
    "flwr[simulation]>=1.32.0",
    "monai>=1.6.0",
    "nibabel>=5.3.3",
    "numpy>=2.4.2",
    "pandas>=3.0.0",
    "pydicom>=3.0.1",
    "torch>=2.10.0,<2.12",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

# On-platform, Flower's runtime dependency installer runs `uv sync` against this
# file on every app launch (ServerApp on the SuperLink, ClientApp on each
# SuperNode) and prepends the resulting per-run env to sys.path. The [tool.uv]
# tables below steer that resolution (FLIP#767):
#   * flip-utils installs from the source baked into the FL images at
#     /opt/flip-utils — never from PyPI — so FL runs always execute the in-repo
#     flip-utils. To test unpublished flip-utils changes, rebuild the FL images
#     (`make build-fl FL_BACKEND=flower`).
#   * torch/torchvision come from PyTorch's cu128 index: PyPI's default cu130
#     wheels need NVIDIA driver >=580 (dev hosts run 575.x). This mirrors the
#     pin in flip-utils' own pyproject, which does NOT apply here — uv honors
#     only the root project's [tool.uv] tables.
# /opt/flip-utils exists only inside the FL containers; for local simulation
# work, start from fl-tutorials/flower/ instead of this template.
[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
flip-utils = { path = "/opt/flip-utils" }
torch = { index = "pytorch-cu128" }
torchvision = { index = "pytorch-cu128" }

[tool.flwr.app]
publisher = "flwrlabs"

# Point to your ServerApp and ClientApp objects
# Format: "<module>:<object>"
[tool.flwr.app.components]
serverapp = "app.server_app:app"
clientapp = "app.client_app:app"

# Custom config values accessible via `context.run_config`
[tool.flwr.app.config]
num-server-rounds = 3 # Equivalent to global_rounds
local-epochs = 1      # Equivalent to local_rounds
learning-rate = 1e-4
val-split = 0.3
test-split = 0.3
batch-size = 2
# Client-side differential privacy, applied by `flip.flower.privacy.flip_local_dp_mod` when the
# uploaded client_app.py registers it on @app.train (see README.md — the template cannot wire it
# itself, because client_app.py is uploaded rather than templated). Declared here so the knobs
# exist for every uploaded app and can be overridden per run; the mod falls back to these same
# defaults when a key is absent.
dp-enabled = true
dp-clipping-norm = 1.0
dp-sensitivity = 1e-4
dp-epsilon = 10.0
dp-delta = 1e-5
# Model configuration
spatial-dims = 3 # This is currently unused but can be passed to get_model()
# FLIP configuration - needs to be set here so it can be overridden by flwr run command
# See https://flower.ai/docs/framework/ref-api-cli.html#flwr-run for more details
flip-model-id = "uuid"
flip-project-id = "uuid"
flip-cohort-query = "*"
flip-job-dir = "job-dir"
# Best-model selection (opt-in): name an aggregated evaluation-metric key the client
# apps report (e.g. "test_loss") to save best_FL_global_model.pt alongside the final
# model; empty disables it. Set -minimize to true when lower is better (e.g. a loss).
best-model-metric = ""
best-model-metric-minimize = false

# Linter
[tool.ruff]
line-length = 120

[tool.ruff.lint]
preview = true
select = ['I', 'F', 'E', 'W', 'PT', 'UP006', 'UP007', 'UP035', 'UP042', 'UP045']
